================================================================================
CLAUDE CODE SESSION -- FOREX BOT REWARD SHAPING (SESSION 9)
================================================================================

--------------------------------------------------------------------------------
[001] YOU
--------------------------------------------------------------------------------
Project root: C:\Users\Adam\Downloads\Main Trading Bot\Projects\Forex Bot

Read src\rl_agent\environment.py in full, then src\rl_agent\trainer.py in full,
then src\rl_agent\final_trainer.py in full.

Make ALL of the following changes:

══════════════════════════════════════════════════════
CHANGE 1 — environment.py: add use_reward_shaping param to __init__
══════════════════════════════════════════════════════

Add   use_reward_shaping: bool = False
as the last parameter of ForexTradingEnv.__init__ before the closing parenthesis.

Then, at the very end of __init__ (after all existing assignments), add:

        # ── Reward shaping (off by default; enabled in final_trainer.py) ─────────
        self._use_reward_shaping  = use_reward_shaping
        self._ENTRY_ZONE_COEF     = 0.12   # max bonus for entry exactly at zone midpoint
        self._ZONE_DIST_CAP       = 1.5    # ATR units beyond which entry bonus is zero
        self._HOLD_WINNER_BONUS   = 0.008  # per-bar bonus while holding unrealized_r > 0.25
        self._EARLY_CUT_BONUS     = 0.025  # bonus for model-initiated close of losing trade

══════════════════════════════════════════════════════
CHANGE 2 — environment.py: capture pre-action state at TOP of step()
══════════════════════════════════════════════════════

Inside step(self, action), add these lines IMMEDIATELY after the method definition
line (and any docstring), BEFORE any action-processing code:

        # ── Snapshot state before action (used by reward shaping at end) ─────────
        _pre_in_trade = self._in_trade
        _bar_before   = self._bar_idx()
        _close_before = float(self._current_row()['close'])
        _zf_before    = self._zone_features[self._current_pair][_bar_before].copy()

══════════════════════════════════════════════════════
CHANGE 3 — environment.py: add shaping block at END of step()
══════════════════════════════════════════════════════

Find the final return statement in step() — the one that returns
(obs, reward, terminated, truncated, info).

IMMEDIATELY BEFORE that return, add:

        # ── Reward shaping ────────────────────────────────────────────────────────
        if self._use_reward_shaping:
            # 1. Zone entry bonus — reward entering near S&R zone midpoint
            if (not _pre_in_trade) and self._in_trade:
                dist = (abs(float(_zf_before[1]))
                        if self._trade_direction == 1
                        else abs(float(_zf_before[25])))
                if dist < self._ZONE_DIST_CAP:
                    reward += self._ENTRY_ZONE_COEF * max(0.0, 1.0 - dist / self._ZONE_DIST_CAP)

            # 2. Hold winner — tiny per-bar bonus for holding a profitable trade
            if _pre_in_trade and self._in_trade:
                if self._unrealized_r(_close_before) > 0.25:
                    reward += self._HOLD_WINNER_BONUS

            # 3. Early cut — bonus for model-initiated close of a losing trade
            if _pre_in_trade and (not self._in_trade) and action == CLOSE:
                if info.get('realized_r', 0.0) < 0.0:
                    reward += self._EARLY_CUT_BONUS

══════════════════════════════════════════════════════
CHANGE 4 — trainer.py: add use_reward_shaping to make_env()
══════════════════════════════════════════════════════

Find the make_env function. Add  use_reward_shaping: bool = False  as a parameter,
and pass it through to ForexTradingEnv inside the inner _init closure.

The result should look like:

def make_env(db_path, pairs, split='train', window_size=252, use_reward_shaping=False):
    def _init():
        env = ForexTradingEnv(
            db_path=db_path, pairs=pairs, split=split,
            window_size=window_size, use_reward_shaping=use_reward_shaping
        )
        return Monitor(RiskGuardWrapper(env))
    return _init

══════════════════════════════════════════════════════
CHANGE 5 — final_trainer.py: enable shaping for training
══════════════════════════════════════════════════════

Find the make_env() call inside run_final_training(). It will look like:
    make_env(str(DB_PATH), all_pairs, split="train")

Change it to:
    make_env(str(DB_PATH), all_pairs, split="train", use_reward_shaping=True)

══════════════════════════════════════════════════════

After all changes, confirm all 5 files were modified successfully.

--------------------------------------------------------------------------------
[002] CLAUDE
--------------------------------------------------------------------------------
Reading all three files in parallel before making changes.

[Read src\rl_agent\environment.py -- full 522 lines]
[Read src\rl_agent\trainer.py -- full 200 lines]
[Read src\rl_agent\final_trainer.py -- full 194 lines]

All three files confirmed. Applied all 5 changes:

CHANGE 1 — environment.py __init__ signature + shaping constants:
- Added use_reward_shaping: bool = False as the last parameter of __init__
  (after date_end=None).
- Added 5-line shaping constants block at the bottom of __init__, after
  self._rng = np.random.default_rng(), before the Data loading section.

CHANGE 2 — environment.py step() pre-action snapshot:
- Inserted 4-line snapshot block (_pre_in_trade, _bar_before, _close_before,
  _zf_before) immediately after the assert line in step(), before
  row = self._current_row().

CHANGE 3 — environment.py step() shaping block before final return:
- Inserted the full if self._use_reward_shaping: block (zone entry bonus,
  hold-winner bonus, early-cut bonus) immediately before the final
  return obs, reward, terminated, truncated, info statement.

CHANGE 4 — trainer.py make_env() signature + inner env + Monitor wrap:
- Changed signature from (db_path, pairs, split='train', window_size=252, seed=None)
  to (db_path, pairs, split='train', window_size=252, use_reward_shaping=False).
- Removed unused seed param; added use_reward_shaping=use_reward_shaping to the
  ForexTradingEnv constructor call.
- Changed return RiskGuardWrapper(env) to return Monitor(RiskGuardWrapper(env)).
- Removed the old docstring from make_env (per the target shape shown).

CHANGE 5 — final_trainer.py make_env() call:
- Changed make_env(str(DB_PATH), all_pairs, split="train")
  to    make_env(str(DB_PATH), all_pairs, split="train", use_reward_shaping=True)
  inside run_final_training().

All 5 changes applied successfully across environment.py, trainer.py,
and final_trainer.py.

--------------------------------------------------------------------------------
[003] YOU
--------------------------------------------------------------------------------
ok. now. in the Past Chats folder, create a new .txt file and name it "session_9".
like usual you're going to put this entire session into the file, everything we ever
said, every text, message, things, all of it that we had in this session into this
file. do it all in the same format as all of the other files that are in the Past
Chats folder(same format as session_1.txt, session_2.txt, session_3.txt, etc and
so on... by reading the other session folder's you should understand how I want/like
it/what I want you to do. basically I always do this to keep a full log of absolutely
everything, even the sessions I have with you but ye.

--------------------------------------------------------------------------------
[004] CLAUDE
--------------------------------------------------------------------------------
Read session_8.txt and the top of session_1.txt from Past Chats to confirm format,
then wrote this file: Past Chats\session_9.txt

Format used (matching all prior sessions):
- Top banner with = separator line, session title in caps, = separator line
- Each turn wrapped in - separator lines with [###] YOU or [###] CLAUDE label
- Full verbatim content of every user message
- CLAUDE turns summarise tool calls in brackets (e.g. [Read file.py -- N lines])
  followed by a plain-text description of what was found and done

================================================================================
END OF SESSION 9
================================================================================
